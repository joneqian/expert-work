# P5a 检索质量 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 记忆检索质量四项——access 强化、importance 进排序、query 改写、abstention 阈值门——在现有架构内落地,不改数据语义(只加一列)。

**Architecture:** 检索排序是单一演进点(SQL `_vector`/`_hybrid` + in-memory 各两处),四项里前两项叠在排序打分上;access 强化加一列 + 命中刷新;后两项(query 改写、abstention)是召回节点 `make_memory_recall_node` 的前后置钩子,不碰 store。三个排序纯函数集中 `common/search/decay.py` 便于单测复用。

**Tech Stack:** SQLAlchemy + pgvector / asyncpg / pytest / prometheus_client。

## Global Constraints

- spec:`docs/superpowers/specs/2026-07-20-memory-evolution-p5-design.md`(P5a 节)。
- 排序公式统一(四处)`final = base × decay × freq_boost(access_count) × imp_weight(importance)`,`base` = vector 路径的 `1−dist/2` 或 hybrid 路径的 RRF `score`。
- 纯函数常数:`freq_boost = min(1.5, 1 + log10(1+access_count)·0.1)`;`imp_weight = 1 + (importance−0.5)·0.4`。
- `access_count`:`int NOT NULL server_default 0`,ge=0。
- migration:`0125_memory_access_count`(24 字符 ≤32),`down_revision = "0124_platform_dynamic_worker"`。
- `bump_access(*, tenant_id, user_id, ids)` 带 tenant+user WHERE(防御,即便运行期 RLS 空转)。
- `abstain_threshold` 默认 `0.0`(= 不 abstain,保守,先埋点观察再收紧)。
- query 改写:fail-open(失败/空→原文)+ 可配置开关(仿 `verify_reads`/`verifier`)。
- 三纯函数集中 `common/search/decay.py`,`__all__` 补全。
- CI:mypy(`packages services/orchestrator/src` + control-plane)、ruff 全库 + `ruff format --check`、pytest 含 `tools/eval/`;integration 需 `export DOCKER_HOST=unix:///Users/mac/.docker/run/docker.sock`。
- IDE 诊断长期 stale,一律以真 pytest/mypy 定论。

---

### Task 1: decay.py 加 frequency_boost + importance_weight 纯函数

**Files:**
- Modify: `packages/expert-work-common/src/expert_work/common/search/decay.py`
- Test: `packages/expert-work-common/tests/test_decay.py`(不存在则 Create)

**Interfaces:**
- Produces:`frequency_boost(access_count: int) -> float`(∈ [1.0, 1.5]);`importance_weight(importance: float) -> float`(∈ [0.8, 1.2])。

- [ ] **Step 1: 写失败测试**

```python
import math

from expert_work.common.search.decay import frequency_boost, importance_weight


def test_frequency_boost_zero_is_neutral() -> None:
    assert frequency_boost(0) == 1.0


def test_frequency_boost_monotone_and_capped() -> None:
    assert frequency_boost(10) == min(1.5, 1 + math.log10(11) * 0.1)
    assert frequency_boost(9) < frequency_boost(90)
    assert frequency_boost(10**9) == 1.5  # cap


def test_frequency_boost_negative_clamped() -> None:
    # defensive: a nonsensical negative count must not blow up log10
    assert frequency_boost(-5) == 1.0


def test_importance_weight_neutral_and_range() -> None:
    assert importance_weight(0.5) == 1.0
    assert importance_weight(1.0) == 1.2
    assert importance_weight(0.0) == 0.8
```

- [ ] **Step 2: 跑失败**：`uv run pytest packages/expert-work-common/tests/test_decay.py -q` → ImportError。

- [ ] **Step 3: 实现**(加在 `temporal_decay_factor` 后)

```python
#: Frequency-reinforcement cap — a heavily-recalled memory tops out at
#: 1.5× so an old-but-hot fact cannot permanently dominate the window.
FREQ_BOOST_CAP = 1.5
#: log-scale reinforcement coefficient (Ebbinghaus-style): ~10 accesses
#: ≈ 1.1×, ~100 ≈ 1.2×. Deliberately gentle — relevance stays the axis.
FREQ_BOOST_K = 0.1


def frequency_boost(access_count: int) -> float:
    """Access-reinforcement multiplier in ``[1.0, FREQ_BOOST_CAP]``.

    ``1 + log10(1 + n)·k`` capped at ``FREQ_BOOST_CAP``. ``n <= 0`` returns
    1.0 (neutral) — a never-recalled memory gets no boost, and a negative
    count (nonsensical) is clamped rather than fed to ``log10``.
    """
    if access_count <= 0:
        return 1.0
    return min(FREQ_BOOST_CAP, 1.0 + math.log10(1 + access_count) * FREQ_BOOST_K)


#: importance re-weight span — importance 0.5 (neutral) → 1.0×, 1.0 → 1.2×,
#: 0.0 → 0.8×. A tie-breaker, never enough to lift a weak-relevance memory
#: over a strong-relevance one.
IMPORTANCE_WEIGHT_SPAN = 0.4


def importance_weight(importance: float) -> float:
    """Importance re-weight in ``[0.8, 1.2]`` centred on 1.0 at importance 0.5."""
    return 1.0 + (importance - 0.5) * IMPORTANCE_WEIGHT_SPAN
```

`import math` 已在文件顶部。`__all__` 改为:
```python
__all__ = [
    "DECAY_FLOOR",
    "DEFAULT_HALF_LIFE",
    "FREQ_BOOST_CAP",
    "FREQ_BOOST_K",
    "IMPORTANCE_WEIGHT_SPAN",
    "frequency_boost",
    "importance_weight",
    "temporal_decay_factor",
]
```

- [ ] **Step 4: 跑通** + `uv run ruff check packages/expert-work-common && uv run mypy packages`。
- [ ] **Step 5: Commit**:`feat: 记忆排序纯函数 frequency_boost + importance_weight`

---

### Task 2: access_count 列(protocol + ORM + migration)

**Files:**
- Modify: `packages/expert-work-protocol/src/expert_work/protocol/memory_item.py`(加字段)
- Modify: `packages/expert-work-persistence/src/expert_work/persistence/models/memory_item.py`(加 ORM 列)
- Modify: `packages/expert-work-persistence/src/expert_work/persistence/memory/sql.py`(`_row_to_item` 映射)
- Create: `packages/expert-work-persistence/migrations/versions/0125_memory_access_count.py`
- Test: `packages/expert-work-protocol/tests/test_memory_item.py`(加断言)

**Interfaces:**
- Produces:`MemoryItem.access_count: int = 0`;`MemoryItemRow.access_count`。

- [ ] **Step 1: 写失败测试**(protocol 默认值)

```python
def test_memory_item_access_count_defaults_zero() -> None:
    from uuid import uuid4

    from expert_work.protocol import MemoryItem

    item = MemoryItem(
        id=uuid4(), tenant_id=uuid4(), user_id=uuid4(), kind="fact",
        content="x", embedding=(0.1,),
    )
    assert item.access_count == 0
```

- [ ] **Step 2: 跑失败** → AttributeError/validation。

- [ ] **Step 3: 实现**
  - protocol `memory_item.py`,在 `last_used_at` 邻位加:
```python
    access_count: int = Field(
        default=0,
        ge=0,
        description="Times this memory has been returned by retrieve (P5a access "
        "reinforcement). Bumped on recall; feeds the frequency boost in ranking "
        "and the consolidator's never-used purge protection.",
    )
```
  - ORM `models/memory_item.py`,在 `last_used_at` 后加:
```python
    # P5a — access reinforcement counter. Bumped on recall; feeds the ranking
    # frequency boost and replaces the (buggy) time-approximation for the
    # consolidator's "never retrieved" purge protection.
    access_count: Mapped[int] = mapped_column(
        Integer, nullable=False, server_default=text("0")
    )
```
    `Integer` 加进顶部 `from sqlalchemy import ...` 导入。
  - `sql.py` `_row_to_item`,加 `access_count=row.access_count,`。
  - migration `0125_memory_access_count.py`:
```python
"""P5a — memory_item.access_count (access reinforcement)."""

from __future__ import annotations

from collections.abc import Sequence

import sqlalchemy as sa
from alembic import op

revision: str = "0125_memory_access_count"
down_revision: str | Sequence[str] | None = "0124_platform_dynamic_worker"
branch_labels: str | Sequence[str] | None = None
depends_on: str | Sequence[str] | None = None

__all__ = ["branch_labels", "depends_on", "down_revision", "downgrade", "revision", "upgrade"]


def upgrade() -> None:
    op.add_column(
        "memory_item",
        sa.Column("access_count", sa.Integer(), nullable=False, server_default=sa.text("0")),
    )


def downgrade() -> None:
    op.drop_column("memory_item", "access_count")
```

- [ ] **Step 4: 跑通** protocol 测 + `uv run ruff check packages && uv run mypy packages`;integration(`export DOCKER_HOST=...`)`uv run pytest packages/expert-work-persistence/tests/test_migrations*.py -q`(若有迁移链测)确认链接 0125。
- [ ] **Step 5: Commit**:`feat: memory_item.access_count 列 + migration 0125`

---

### Task 3: MemoryStore.bump_access(base + sql + in-memory)

**Files:**
- Modify: `packages/expert-work-persistence/src/expert_work/persistence/memory/base.py`(抽象方法)
- Modify: `packages/expert-work-persistence/src/expert_work/persistence/memory/sql.py`(批量 UPDATE)
- Modify: `packages/expert-work-persistence/src/expert_work/persistence/memory/memory.py`(in-memory)
- Test: `packages/expert-work-persistence/tests/test_memory_store.py`(加,或就近既有 store 测)

**Interfaces:**
- Consumes: Task 2 列。
- Produces:`async def bump_access(self, *, tenant_id: UUID, user_id: UUID, ids: Sequence[UUID]) -> None`(空 ids no-op;`last_used_at=now, access_count+=1`,tenant+user 作防御 WHERE)。

- [ ] **Step 1: 写失败测试**(in-memory)

```python
@pytest.mark.asyncio
async def test_bump_access_increments_and_touches() -> None:
    from datetime import UTC, datetime
    from uuid import uuid4

    from expert_work.persistence.memory.memory import InMemoryMemoryStore
    from expert_work.protocol import MemoryItem

    t, u = uuid4(), uuid4()
    mid = uuid4()
    store = InMemoryMemoryStore()
    old = datetime(2020, 1, 1, tzinfo=UTC)
    await store.write([MemoryItem(id=mid, tenant_id=t, user_id=u, kind="fact",
                                  content="x", embedding=(0.1,), last_used_at=old,
                                  access_count=0)])
    await store.bump_access(tenant_id=t, user_id=u, ids=[mid])
    row = (await store.list_for_user(tenant_id=t, user_id=u))[0]
    assert row.access_count == 1
    assert row.last_used_at > old


@pytest.mark.asyncio
async def test_bump_access_empty_ids_noop() -> None:
    from uuid import uuid4

    from expert_work.persistence.memory.memory import InMemoryMemoryStore

    await InMemoryMemoryStore().bump_access(tenant_id=uuid4(), user_id=uuid4(), ids=[])
```

- [ ] **Step 2: 跑失败** → AttributeError。

- [ ] **Step 3: 实现**
  - base.py 抽象(邻 `soft_delete`):
```python
    @abc.abstractmethod
    async def bump_access(
        self, *, tenant_id: UUID, user_id: UUID, ids: Sequence[UUID]
    ) -> None:
        """P5a — mark memories as retrieved: stamp ``last_used_at=now`` and
        increment ``access_count`` for each id. Best-effort at the call site
        (recall must not fail on a bump error). Empty ``ids`` is a no-op.
        Tenant- AND user-scoped as a defensive predicate."""
```
  - sql.py:
```python
    async def bump_access(
        self, *, tenant_id: UUID, user_id: UUID, ids: Sequence[UUID]
    ) -> None:
        if not ids:
            return
        now = datetime.now(UTC)
        stmt = (
            update(MemoryItemRow)
            .where(
                MemoryItemRow.tenant_id == tenant_id,
                MemoryItemRow.user_id == user_id,
                MemoryItemRow.id.in_(list(ids)),
            )
            .values(last_used_at=now, access_count=MemoryItemRow.access_count + 1)
        )
        async with self._sf() as session:
            await session.execute(stmt)
            await session.commit()
```
  - in-memory memory.py:
```python
    async def bump_access(
        self, *, tenant_id: UUID, user_id: UUID, ids: Sequence[UUID]
    ) -> None:
        if not ids:
            return
        now = datetime.now(UTC)
        id_set = set(ids)
        self._rows = [
            row.model_copy(
                update={"last_used_at": now, "access_count": row.access_count + 1}
            )
            if (row.tenant_id == tenant_id and row.user_id == user_id and row.id in id_set)
            else row
            for row in self._rows
        ]
```
  - 其余 `MemoryStore` 子类(若测试桩/别处有)补 no-op 或实现——grep `class .*\(MemoryStore\)` 确认全覆盖(否则 abstractmethod 报错)。

- [ ] **Step 4: 跑通** + ruff + mypy packages;grep 确认无遗漏子类。
- [ ] **Step 5: Commit**:`feat: MemoryStore.bump_access(命中刷新 last_used_at + access_count)`

---

### Task 4: 检索排序叠 frequency_boost + importance_weight(四处)

**Files:**
- Modify: `packages/expert-work-persistence/src/expert_work/persistence/memory/sql.py`(`_vector_retrieve` 227-234 / `_hybrid_retrieve` 298-305)
- Modify: `packages/expert-work-persistence/src/expert_work/persistence/memory/memory.py`(in-memory retrieve 154-158 / 162-169)
- Test: 就近 store 检索测

**Interfaces:**
- Consumes: Task 1 纯函数 + Task 2 列。

- [ ] **Step 1: 写失败测试**(同 similarity 下高 access/importance 排前)

```python
@pytest.mark.asyncio
async def test_ranking_reinforces_access_and_importance() -> None:
    from uuid import uuid4

    from expert_work.persistence.memory.memory import InMemoryMemoryStore
    from expert_work.protocol import MemoryItem

    t, u = uuid4(), uuid4()
    emb = (1.0, 0.0)
    store = InMemoryMemoryStore()
    low = MemoryItem(id=uuid4(), tenant_id=t, user_id=u, kind="fact", content="a",
                     embedding=emb, access_count=0, importance=0.5)
    hot = MemoryItem(id=uuid4(), tenant_id=t, user_id=u, kind="fact", content="b",
                     embedding=emb, access_count=100, importance=0.9)
    await store.write([low, hot])
    got = await store.retrieve(tenant_id=t, user_id=u, query_embedding=emb, limit=2)
    assert got[0].content == "b"  # same cosine, hot+important wins
```

- [ ] **Step 2: 跑失败**(现排序无 access/importance,顺序不定/按插入)。

- [ ] **Step 3: 实现**——四处排序 key 统一叠两因子。sql.py 顶部导入加 `frequency_boost, importance_weight`。
  - `_vector_retrieve`(227-234)key 改:
```python
            key=lambda row: (
                (1.0 - _cosine_distance_value(query_embedding, row.embedding) / 2.0)
                * _decay_for(row.last_used_at, row.created_at, now=now)
                * frequency_boost(row.access_count)
                * importance_weight(row.importance)
            ),
```
  - `_hybrid_retrieve`(298-305):
```python
                (mid, score
                 * _decay_for(by_id[mid].last_used_at, by_id[mid].created_at, now=now)
                 * frequency_boost(by_id[mid].access_count)
                 * importance_weight(by_id[mid].importance))
```
  - in-memory memory.py 顶部导入加两函数;hybrid(154-158):
```python
                ((row, score
                  * _decay_for(row, now=now)
                  * frequency_boost(row.access_count)
                  * importance_weight(row.importance)) for row, score in scored),
```
  - in-memory vector(162-169)key:
```python
            key=lambda row: (
                (1.0 - _cosine_distance(query_embedding, row.embedding) / 2.0)
                * _decay_for(row, now=now)
                * frequency_boost(row.access_count)
                * importance_weight(row.importance)
            ),
```

- [ ] **Step 4: 跑通** in-memory 测 + integration(DOCKER_HOST)sql 检索测 + ruff + mypy。
- [ ] **Step 5: Commit**:`feat: 检索排序纳入 access 频次 + importance 权重(四处)`

---

### Task 5: 召回节点命中后 bump_access

**Files:**
- Modify: `services/orchestrator/src/orchestrator/graph_builder/memory.py`(`memory_recall_node` 最终 memories 后)
- Test: `services/orchestrator/tests/test_memory_recall*.py`(就近)

**Interfaces:**
- Consumes: Task 3 `bump_access`。

- [ ] **Step 1: 写失败测试**：召回后 store 对被召回 id 的 `access_count` 加 1(用 InMemoryMemoryStore + 真召回节点,断言 bump)。

- [ ] **Step 2: 跑失败**。

- [ ] **Step 3: 实现**——在 `memory_recall_node` 拿到最终 `memories`(redact 前,用真实 id)后、`return {"recalled_memories": ...}` 前,best-effort bump:
```python
        if memories:
            try:
                await memory_store.bump_access(
                    tenant_id=tenant_id,
                    user_id=user_id,
                    ids=[m.id for m in memories],
                )
            except RunCancelledError:
                raise
            except Exception:
                logger.warning("memory.bump_access_failed", exc_info=True)
```
  说明:`await` 而非 `create_task`(避免 graph 节点返回后 orphan);一条批量 UPDATE ms 级,不算阻塞;不进 `token.run_cancellable`(bump 不该被取消打断,但取消异常仍透传)。

- [ ] **Step 4: 跑通** `uv run pytest services/orchestrator/tests/test_memory_recall*.py -q` + ruff + mypy(含 orchestrator)。
- [ ] **Step 5: Commit**:`feat: 召回命中后 best-effort bump_access(激活衰减锚)`

---

### Task 6: consolidator never-used 判定改 access_count

**Files:**
- Modify: `packages/expert-work-persistence/src/expert_work/persistence/memory/sql.py`(`list_purge_candidates` 588-600)
- Modify: `packages/expert-work-persistence/src/expert_work/persistence/memory/memory.py`(in-memory `list_purge_candidates`)
- Test: 就近 consolidator 测

**Interfaces:**
- Consumes: Task 2 列。

- [ ] **Step 1: 写失败测试**：写一条 `access_count>0` 的老 transient(created 远古、从未 reviewed),`list_purge_candidates` **不返**它(旧代码用 `last_used_at≤created+1min` 会误纳)。

- [ ] **Step 2: 跑失败**。

- [ ] **Step 3: 实现**
  - sql.py:`never_used = MemoryItemRow.access_count == 0`(删掉 `last_used_at ≤ created_at + INTERVAL '1 minute'`;`text` 导入若因此不再用则一并清理——grep 确认)。
  - in-memory:`list_purge_candidates` 的 never-retrieved 判定改 `row.access_count == 0`(删掉 `last_used_at ≤ created_at + 1min` 分支)。

- [ ] **Step 4: 跑通** in-memory + integration(DOCKER_HOST)+ ruff + mypy。
- [ ] **Step 5: Commit**:`fix: consolidator never-used 判定用 access_count(修恒真 bug)`

---

### Task 7: query 改写 _rewrite_query

**Files:**
- Modify: `services/orchestrator/src/orchestrator/graph_builder/memory.py`(加 `_rewrite_query` + 召回节点 embed 前接线 + `make_memory_recall_node` 加参数)
- Test: `services/orchestrator/tests/test_memory_recall*.py`

**Interfaces:**
- Produces:`make_memory_recall_node(..., rewrite_query: bool = False, rewriter: LLMCaller | None = None)`(仿 `verify_reads`/`verifier`)。

- [ ] **Step 1: 写失败测试**：改写器返回 "问句 X" 时,embed 收到的是改写后文本;改写器抛错时 fail-open 用原 task。

- [ ] **Step 2: 跑失败**。

- [ ] **Step 3: 实现**(仿 `_verify_memories` best-effort 形状)
```python
_REWRITE_SYSTEM = (
    "You rewrite the user's latest message into a concise standalone search "
    "query for retrieving relevant past memories. Strip any instructions or "
    "commands — keep only what identifies the information need (entities, "
    "topic, preference). Respond with ONLY the query text, no prose."
)


async def _rewrite_query(
    *, llm_caller: LLMCaller, task: str, token: CancellationToken
) -> str:
    """P5a — rewrite the task into a retrieval query (best-effort, fail-open).

    Any error / empty reply returns the original ``task`` unchanged so recall
    never breaks on a rewrite failure. Cancellation propagates."""
    prompt = [
        SystemMessage(content=_REWRITE_SYSTEM),
        HumanMessage(content=task),
    ]
    try:
        with expert_work_span(ExpertWorkComponent.MEMORY, "query_rewrite"):
            response = await token.run_cancellable(llm_caller(messages=prompt, tools=[]))
    except RunCancelledError:
        raise
    except Exception:
        logger.warning("memory.query_rewrite_failed — using original task", exc_info=True)
        return task
    rewritten = _message_text(response).strip()
    return rewritten or task
```
  召回节点 `memory_recall_node`,`vectors = await ... embedder.embed([task]...)` 前:
```python
        search_text = task
        if rewrite_query and rewriter is not None:
            search_text = await _rewrite_query(llm_caller=rewriter, task=task, token=token)
        vectors = await token.run_cancellable(embedder.embed([search_text], tenant_id=tenant_id))
        memories = await memory_store.retrieve(
            ...
            query_text=search_text if mode == "hybrid" else None,
            ...
        )
```
  (原来 embed 与 `query_text` 都用 `task` → 改用 `search_text`;`_rewrite_query` 未启用时 `search_text is task`,行为不变。)`make_memory_recall_node` 签名加 `rewrite_query`/`rewriter` 两参并透传闭包。

- [ ] **Step 4: 跑通** + ruff + mypy(orchestrator)。
- [ ] **Step 5: Commit**:`feat: 记忆检索 query 改写(aux LLM,fail-open,可配置)`

---

### Task 8: abstention 阈值门 + record_memory_abstain

**Files:**
- Modify: `packages/expert-work-common/src/expert_work/common/uplift_metrics.py`(counter + `record_memory_abstain`)
- Modify: `services/orchestrator/src/orchestrator/graph_builder/memory.py`(召回节点 retrieve 后阈值门 + `make_memory_recall_node` 加 `abstain_threshold`)
- Test: `services/orchestrator/tests/test_memory_recall*.py`

**Interfaces:**
- Produces:`record_memory_abstain() -> None`;`make_memory_recall_node(..., abstain_threshold: float = 0.0)`。

- [ ] **Step 1: 写失败测试**：候选最高 cosine similarity < 阈值 → 召回返空(`recalled_memories` 空)+ `record_memory_abstain` 触发;≥ 阈值 → 正常;默认 0.0 → 从不 abstain。

- [ ] **Step 2: 跑失败**。

- [ ] **Step 3: 实现**
  - uplift_metrics.py(仿 `record_memory_verify` + 其 counter 定义):
```python
_memory_abstain_total = Counter(
    "expert_work_memory_abstain_total",
    "Recalls that returned empty because the top candidate's similarity fell "
    "below the abstention threshold (P5a).",
)


def record_memory_abstain() -> None:
    """Bump ``expert_work_memory_abstain_total`` (P5a abstention gate)."""
    _memory_abstain_total.inc()
```
  (Counter 定义放该文件既有 `_memory_*_total` 声明区;`Counter` 已导入。)
  - 召回节点:`retrieve` 返回 `memories` 后、rerank 前加阈值门。`_reconcile_cosine`(本文件 538-546)**已返回余弦相似度 ∈ [−1,1]**(非距离),直接复用,不新增 helper:
```python
        if memories and abstain_threshold > 0.0:
            top_sim = max((_reconcile_cosine(vectors[0], m.embedding) for m in memories), default=0.0)
            if top_sim < abstain_threshold:
                record_memory_abstain()
                logger.info("memory.abstain top_sim=%.3f < %.3f", top_sim, abstain_threshold)
                return {}
```
  阈值门放 retrieve 后、rerank 前,省下不相关时的 rerank/mmr/verify 开销。`make_memory_recall_node` 加 `abstain_threshold: float = 0.0` 透传。

- [ ] **Step 4: 跑通** + ruff 全库 + mypy(common + orchestrator)。
- [ ] **Step 5: Commit**:`feat: 记忆召回 abstention 阈值门 + record_memory_abstain 指标`

---

## Self-Review

- **spec 覆盖**:P5a 四项 ① access 强化(T1 纯函数 + T2 列 + T3 bump + T4 排序 + T5 召回接线 + T6 never-used 修复)、② importance 排序(T1 + T4)、③ query 改写(T7)、④ abstention(T8)。全覆盖。
- **占位扫描**:无 TBD;`bump_access` 子类全覆盖、`text` 导入清理在 T6 标了 grep 确认。
- **类型一致**:`frequency_boost(int)->float`、`importance_weight(float)->float`、`bump_access(*,tenant_id,user_id,ids)` 全链一致;四处排序公式同构;`make_memory_recall_node` 新增三参(rewrite_query/rewriter/abstain_threshold)仿既有 verify_reads/verifier 形状。
- **落点核对**:排序四处行号(sql 227-234/298-305、in-memory 154-158/162-169)、召回节点、`list_purge_candidates`(sql 588-600)均实读确认。
- **配置接线**:rewrite_query/rewriter/abstain_threshold 三参谁传入(平台/agent 级)——透传到 `make_memory_recall_node` 的上游装配点(`agent_factory` 建召回节点处)在实现时按既有 `verify_reads` 的装配范式接;默认关/0.0 保证不接线时行为不变。
