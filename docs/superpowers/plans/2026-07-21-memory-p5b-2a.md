# Memory P5b-2a(as_of 时间旅行检索 + DLQ 溯源补齐)Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 给 `retrieve()` 加可选 `as_of` 时间旅行参数(查"某时刻当时有效的记忆"),并把 P5b-1 暂缓的 DLQ 重试路径 `source_run_id` 溯源补齐。

**Architecture:** as_of 建在 P5b-1 已有的 valid_at/invalid_at/expired_at 列上——`as_of=None`(默认)= 当前有效(P5b-1 行为不变);`as_of=<t>` = `valid_at<=t AND (invalid_at IS NULL OR invalid_at>t) AND (expired_at IS NULL OR expired_at>t)`。两 store parity。DLQ 溯源沿 P5b-1 的 `source_run_id` 一路补到重试路径(dlq 表加列 + enqueue 透传 + worker 重建时填)。

**Tech Stack:** Python 3.13 / SQLAlchemy async + pgvector / Alembic / pytest(单测 + `DOCKER_HOST` 集成)。

## Global Constraints

- 新 migration `0128_dlq_source_run_id`(≤32 字符);`down_revision = "0127_memory_dedup_bitemporal"`(当前 head,写 plan 时已确认)。列 nullable 非阻塞。
- **两 store 全程 parity**:as_of 的过滤改动必须同时落 `SqlMemoryStore`(`memory/sql.py`)与 `InMemoryMemoryStore`(`memory/memory.py`),语义一致。
- **`as_of=None` 默认零行为变化**:现有 2 个 `retrieve` 调用点(`graph_builder/memory.py:531` recall、`:697` reconcile neighbor)不传 as_of,行为必须与 P5b-1 逐字节一致。
- **as_of 仍排除 `deleted_at IS NOT NULL`**(软删=用户遗忘,隐私,即使时间旅行也不返)与 status(archived/consolidated)过滤;as_of 只时间旅行 valid/invalid/expired 三维。
- **store 层新能力必须补 committed SQL 真容器集成测 + 变异自验**(P5b-1 教训:in-memory-only 漏 SQL 特有 bug)。
- **DLQ 溯源**:`source_run_id` 从 `flush_messages_to_memory` 的 enqueue 调用一路透传到 dlq 行,worker 重建 `MemoryItem` 时填。`source_run_id` 用 Text(与 `source_thread_id` 一致,mirror P5b-1)。
- CI:mypy 范围含 `packages` + `services/orchestrator/src` + control-plane services;ruff 全库 + `format --check`;pytest 含 `tools/eval`。集成测需 `export DOCKER_HOST=unix:///Users/mac/.docker/run/docker.sock`。
- **不做(P5b-2b / 2c / backlog)**:⑦ `expected_valid_days` 主动预测复核、⑧ 抽取 prompt correction 标记、前端记忆 tab、把 as_of 暴露给 API/前端(本 PR 只落 store 能力 + 测试,像 P5a 落 rewrite/abstain 于 node 层后再接)。

---

### Task 1: `retrieve` 加 `as_of` 时间旅行(两 store)

**Files:**
- Modify: `packages/expert-work-persistence/src/expert_work/persistence/memory/base.py`(抽象签名)
- Modify: `packages/expert-work-persistence/src/expert_work/persistence/memory/sql.py`(`_retrieve_filter` 参数化 + `retrieve`/`_vector_retrieve`/`_hybrid_retrieve` 透传)
- Modify: `packages/expert-work-persistence/src/expert_work/persistence/memory/memory.py`(`retrieve` candidates 分支)
- Test: `packages/expert-work-persistence/tests/test_in_memory_memory_store.py`(快)+ `tests/test_sql_memory_store.py`(集成 + 变异)

**Interfaces:**
- Produces: `MemoryStore.retrieve(..., as_of: datetime | None = None)` 三签名(base/sql/in-memory)一致。`as_of=None` → 当前有效过滤(P5b-1 不变);`as_of=<t>` → `(valid_at IS NULL OR valid_at<=t) AND (invalid_at IS NULL OR invalid_at>t) AND (expired_at IS NULL OR expired_at>t)`。sql 侧 `_retrieve_filter(as_of: datetime | None = None)` 返回 status 谓词 + 对应时间谓词。

- [ ] **Step 1: 写失败测试**

**主测(in-memory,快)** 追加到 `test_in_memory_memory_store.py`:

```python
async def test_retrieve_as_of_returns_historical_state() -> None:
    from datetime import UTC, datetime
    from uuid import uuid4

    from expert_work.persistence.memory.memory import InMemoryMemoryStore
    from expert_work.protocol import MemoryItem

    store = InMemoryMemoryStore()
    tenant, user = uuid4(), uuid4()
    t_jan = datetime(2026, 1, 1, tzinfo=UTC)
    t_mar = datetime(2026, 3, 1, tzinfo=UTC)
    t_jun = datetime(2026, 6, 1, tzinfo=UTC)

    # "Beijing" valid Jan, superseded by "Shanghai" in Mar.
    beijing = MemoryItem(
        id=uuid4(), tenant_id=tenant, user_id=user, kind="fact",
        content="user lives in Beijing", embedding=(1.0, 0.0, 0.0),
        valid_at=t_jan, invalid_at=t_mar,
    )
    shanghai = MemoryItem(
        id=uuid4(), tenant_id=tenant, user_id=user, kind="fact",
        content="user lives in Shanghai", embedding=(1.0, 0.0, 0.0),
        valid_at=t_mar,
    )
    await store.write([beijing])
    await store.write([shanghai])

    # Current (as_of=None): only Shanghai (Beijing is invalid_at set).
    now_hits = await store.retrieve(
        tenant_id=tenant, user_id=user, query_embedding=(1.0, 0.0, 0.0), limit=10
    )
    assert [m.content for m in now_hits] == ["user lives in Shanghai"]

    # As of Feb (between Jan valid and Mar supersede): only Beijing was valid.
    feb = datetime(2026, 2, 1, tzinfo=UTC)
    feb_hits = await store.retrieve(
        tenant_id=tenant, user_id=user, query_embedding=(1.0, 0.0, 0.0), limit=10, as_of=feb
    )
    assert [m.content for m in feb_hits] == ["user lives in Beijing"]

    # As of Jun: Shanghai (Beijing already superseded, Shanghai valid since Mar).
    jun_hits = await store.retrieve(
        tenant_id=tenant, user_id=user, query_embedding=(1.0, 0.0, 0.0), limit=10, as_of=t_jun
    )
    assert [m.content for m in jun_hits] == ["user lives in Shanghai"]


async def test_retrieve_as_of_excludes_not_yet_valid() -> None:
    from datetime import UTC, datetime
    from uuid import uuid4

    from expert_work.persistence.memory.memory import InMemoryMemoryStore
    from expert_work.protocol import MemoryItem

    store = InMemoryMemoryStore()
    tenant, user = uuid4(), uuid4()
    # valid_at in the future relative to the as_of query → not yet known.
    await store.write([
        MemoryItem(
            id=uuid4(), tenant_id=tenant, user_id=user, kind="fact",
            content="future fact", embedding=(1.0, 0.0, 0.0),
            valid_at=datetime(2026, 5, 1, tzinfo=UTC),
        )
    ])
    hits = await store.retrieve(
        tenant_id=tenant, user_id=user, query_embedding=(1.0, 0.0, 0.0),
        as_of=datetime(2026, 2, 1, tzinfo=UTC),
    )
    assert hits == []
```

**SQL 集成测** 追加到 `test_sql_memory_store.py`(用 `sql_store` fixture `(store, engine)` + `_vec`;直建 `MemoryItem`):

```python
@pytest.mark.asyncio
async def test_retrieve_as_of_time_travel_sql(sql_store: SqlStoreFixture) -> None:
    from datetime import UTC, datetime

    store, _engine = sql_store
    tenant, user = uuid4(), uuid4()
    t_jan = datetime(2026, 1, 1, tzinfo=UTC)
    t_mar = datetime(2026, 3, 1, tzinfo=UTC)
    await store.write([
        MemoryItem(
            id=uuid4(), tenant_id=tenant, user_id=user, kind="fact",
            content="user lives in Beijing", embedding=_vec(1.0, 0.0),
            valid_at=t_jan, invalid_at=t_mar,
        )
    ])
    await store.write([
        MemoryItem(
            id=uuid4(), tenant_id=tenant, user_id=user, kind="fact",
            content="user lives in Shanghai", embedding=_vec(1.0, 0.0),
            valid_at=t_mar,
        )
    ])
    # Current: Shanghai only.
    now_hits = await store.retrieve(
        tenant_id=tenant, user_id=user, query_embedding=_vec(1.0, 0.0), limit=10
    )
    assert [m.content for m in now_hits] == ["user lives in Shanghai"]
    # As of Feb: Beijing only.
    feb_hits = await store.retrieve(
        tenant_id=tenant, user_id=user, query_embedding=_vec(1.0, 0.0), limit=10,
        as_of=datetime(2026, 2, 1, tzinfo=UTC),
    )
    assert [m.content for m in feb_hits] == ["user lives in Beijing"]
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd packages/expert-work-persistence && uv run pytest tests/test_in_memory_memory_store.py -k as_of -q`
Expected: FAIL(`retrieve() got an unexpected keyword argument 'as_of'`)。

- [ ] **Step 3: base 抽象签名加 as_of**

`base.py` `retrieve` 抽象方法签名在 `limit: int = 5,` 之前加 `as_of: datetime | None = None,`,并在 docstring 末尾加一段:

```
        Stream P5b — ``as_of`` (when given) time-travels retrieval: instead of
        "currently valid" it returns rows that were valid AT that instant
        (``valid_at <= as_of`` and neither superseded nor expired as of then).
        ``None`` (default) is the current-time behaviour. ``deleted_at`` and
        the archived/consolidated filters still apply — as_of only time-travels
        the bi-temporal (valid/invalid/expired) dimension.
```

（`datetime` 需 import:`from datetime import datetime` —— 若 base.py 未 import,加它。）

- [ ] **Step 4: sql `_retrieve_filter` 参数化 + 透传**

`sql.py`:
1. `_retrieve_filter` 改签名 + 分支:

```python
def _retrieve_filter(as_of: datetime | None = None) -> list[ColumnElement[bool]]:
    base: list[ColumnElement[bool]] = [
        MemoryItemRow.status != "archived",
        or_(
            MemoryItemRow.status == "consolidated",
            MemoryItemRow.consolidated_into.is_(None),
        ),
    ]
    if as_of is None:
        # Current-time: hide superseded (invalid_at) + world-expired rows.
        base.append(MemoryItemRow.invalid_at.is_(None))
        base.append(
            or_(MemoryItemRow.expired_at.is_(None), MemoryItemRow.expired_at > func.now())
        )
    else:
        # Time-travel: what was valid AT ``as_of``.
        base.append(
            or_(MemoryItemRow.valid_at.is_(None), MemoryItemRow.valid_at <= as_of)
        )
        base.append(
            or_(MemoryItemRow.invalid_at.is_(None), MemoryItemRow.invalid_at > as_of)
        )
        base.append(
            or_(MemoryItemRow.expired_at.is_(None), MemoryItemRow.expired_at > as_of)
        )
    return base
```

2. `retrieve` 签名加 `as_of: datetime | None = None,`(在 `limit` 前),并把 as_of 传给 `_hybrid_retrieve`/`_vector_retrieve`。
3. `_vector_retrieve` + `_hybrid_retrieve` 各加 `as_of: datetime | None = None,` 参数,并把它们的 `*_retrieve_filter()` 调用改成 `*_retrieve_filter(as_of)`。

- [ ] **Step 5: in-memory `retrieve` 分支**

`memory.py` `retrieve` 签名加 `as_of: datetime | None = None,`(在 `limit` 前)。candidates 列表推导里现有的两行 bi-temporal 过滤(`row.invalid_at is None` + `row.expired_at is None or row.expired_at > now`)替换为一个按 as_of 分支的谓词。最清晰做法:candidates 推导前算一个 helper:

```python
        def _temporally_visible(row: MemoryItem) -> bool:
            if as_of is None:
                return row.invalid_at is None and (
                    row.expired_at is None or row.expired_at > now
                )
            return (
                (row.valid_at is None or row.valid_at <= as_of)
                and (row.invalid_at is None or row.invalid_at > as_of)
                and (row.expired_at is None or row.expired_at > as_of)
            )
```

然后 candidates 推导里把那两行换成 `and _temporally_visible(row)`。（`now = datetime.now(UTC)` 已在方法开头,P5b-1 已上移。）

- [ ] **Step 6: 跑测试确认通过(in-memory)**

Run: `cd packages/expert-work-persistence && uv run pytest tests/test_in_memory_memory_store.py -q`
Expected: PASS（含既有 retrieve 测试不回退 —— as_of=None 路径逐字节等价）。

- [ ] **Step 7: 跑 SQL 集成测 + 变异自验**

Run: `export DOCKER_HOST=unix:///Users/mac/.docker/run/docker.sock && cd packages/expert-work-persistence && uv run pytest tests/test_sql_memory_store.py -k "as_of or bitemporal or excludes" -q`
Expected: PASS。
**变异自验**:临时把 `_retrieve_filter` 的 as_of 分支 `MemoryItemRow.invalid_at > as_of` 改成 `< as_of`,重跑,确认 `test_retrieve_as_of_time_travel_sql` FAIL(Feb 查询会漏 Beijing 或多返 Shanghai);再精确 revert,确认 GREEN + `git diff` on sql.py 空。

- [ ] **Step 8: commit**

```bash
git add packages/expert-work-persistence/src/expert_work/persistence/memory/base.py packages/expert-work-persistence/src/expert_work/persistence/memory/sql.py packages/expert-work-persistence/src/expert_work/persistence/memory/memory.py packages/expert-work-persistence/tests/test_in_memory_memory_store.py packages/expert-work-persistence/tests/test_sql_memory_store.py
git commit -m "feat(memory): P5b as_of 时间旅行检索(两 store)"
```

---

### Task 2: DLQ 行加 `source_run_id`(ORM + migration + DLQRow + `_row_to_dlq`)

**Files:**
- Modify: `packages/expert-work-persistence/src/expert_work/persistence/models/memory_writeback_dlq.py`
- Create: `packages/expert-work-persistence/migrations/versions/0128_dlq_source_run_id.py`
- Modify: `packages/expert-work-persistence/src/expert_work/persistence/memory/dlq.py`(`DLQRow` + `_row_to_dlq`)
- Test: `packages/expert-work-persistence/tests/`(既有 dlq 测试文件;先 `grep -rln "MemoryWritebackDLQ\|DLQRow" packages/expert-work-persistence/tests/`)

**Interfaces:**
- Produces: `MemoryWritebackDLQRow.source_run_id: Mapped[str | None]`(Text nullable,migration 0128);`DLQRow.source_run_id: str | None` 字段;`_row_to_dlq` 映射它。

- [ ] **Step 1: 写失败测试**

先探明既有 dlq 测试文件(`grep -rln "MemoryWritebackDLQ\|DLQRow\|enqueue" packages/expert-work-persistence/tests/`)。在其末尾加(否则新建 `test_dlq_source_run_id.py`)—— **直接构造 `DLQRow` 只测本 task 的 dataclass 字段**(不经 `enqueue`,那是 Task 3):

```python
def test_dlqrow_has_source_run_id_field() -> None:
    from datetime import UTC, datetime
    from uuid import uuid4

    from expert_work.persistence.memory.dlq import DLQRow

    row = DLQRow(
        id=uuid4(),
        tenant_id=uuid4(),
        user_id=uuid4(),
        source_thread_id="t",
        source_run_id="run-123",
        extracted=(("fact", "x"),),
        attempts=0,
        next_retry_at=datetime.now(UTC),
        last_error=None,
        created_at=datetime.now(UTC),
    )
    assert row.source_run_id == "run-123"
```

（`DLQRow` 是 `@dataclass(frozen=True)`,新字段是位置参数 —— 上面按声明顺序 `source_thread_id, source_run_id, extracted, …` 构造。）

- [ ] **Step 2: 跑测试确认失败**

Run: `cd packages/expert-work-persistence && uv run pytest -k dlqrow_has_source_run_id -q`
Expected: FAIL(`DLQRow.__init__() got an unexpected keyword argument 'source_run_id'` / 参数不匹配)。

- [ ] **Step 3: ORM 加列**

`memory_writeback_dlq.py` 在 `source_thread_id` 之后加:

```python
    #: Stream P5b — the run (agent_run.id, stringified) that produced these
    #: extracted memories. Carried through so a retried writeback keeps run
    #: provenance. NULL for legacy rows.
    source_run_id: Mapped[str | None] = mapped_column(Text, nullable=True)
```

（`Text` 已 import。）

- [ ] **Step 4: migration 0128**

Create `packages/expert-work-persistence/migrations/versions/0128_dlq_source_run_id.py`:

```python
"""P5b — memory_writeback_dlq.source_run_id (DLQ 溯源补齐).

Revision ID: 0128_dlq_source_run_id
Revises: 0127_memory_dedup_bitemporal
Create Date: 2026-07-21
"""

from __future__ import annotations

import sqlalchemy as sa
from alembic import op

revision = "0128_dlq_source_run_id"
down_revision = "0127_memory_dedup_bitemporal"
branch_labels = None
depends_on = None


def upgrade() -> None:
    op.add_column(
        "memory_writeback_dlq", sa.Column("source_run_id", sa.Text(), nullable=True)
    )


def downgrade() -> None:
    op.drop_column("memory_writeback_dlq", "source_run_id")
```

- [ ] **Step 5: `DLQRow` + `_row_to_dlq`**

`dlq.py`:
1. `DLQRow` dataclass 加字段 `source_run_id: str | None`(放在 `source_thread_id` 之后,保持字段顺序可读):

```python
    source_thread_id: str | None
    source_run_id: str | None
    extracted: tuple[tuple[str, str], ...]
```

2. `_row_to_dlq` 加 `source_run_id=row.source_run_id,`(在 `source_thread_id=` 之后)。

- [ ] **Step 6: 跑测试确认通过**

Run: `cd packages/expert-work-persistence && uv run pytest -k dlqrow_has_source_run_id -q`
Expected: PASS（dataclass 字段测;`enqueue` 透传由 Task 3、migration 由 Task 4 的端到端集成测覆盖)。

- [ ] **Step 7: commit**

```bash
git add packages/expert-work-persistence/src/expert_work/persistence/models/memory_writeback_dlq.py packages/expert-work-persistence/migrations/versions/0128_dlq_source_run_id.py packages/expert-work-persistence/src/expert_work/persistence/memory/dlq.py packages/expert-work-persistence/tests/
git commit -m "feat(memory): P5b DLQ 行加 source_run_id(ORM + migration 0128 + DLQRow)"
```

---

### Task 3: DLQ `enqueue` 透传 `source_run_id`(abstract + 两 impl)

**Files:**
- Modify: `packages/expert-work-persistence/src/expert_work/persistence/memory/dlq.py`
- Test: 同 Task 2 的 dlq 测试文件

**Interfaces:**
- Consumes: `DLQRow.source_run_id`(Task 2)、ORM 列(Task 2)。
- Produces: `MemoryWritebackDLQ.enqueue(..., source_run_id: str | None = None, ...)` 三签名(abstract + in-memory + sql)一致,把 `source_run_id` 落进 `DLQRow` / 表行。默认 `None` 向后兼容。

- [ ] **Step 1: 写失败测试**

在 dlq 测试文件加(用 in-memory,快):

```python
async def test_enqueue_persists_source_run_id() -> None:
    from uuid import uuid4

    from expert_work.persistence.memory.dlq import InMemoryMemoryWritebackDLQ

    dlq = InMemoryMemoryWritebackDLQ()
    run = uuid4()
    row = await dlq.enqueue(
        tenant_id=uuid4(), user_id=uuid4(),
        source_thread_id="t", source_run_id=str(run),
        extracted=[("fact", "x")], error="e",
    )
    assert row.source_run_id == str(run)
    ready = await dlq.take_ready(limit=10, now=row.next_retry_at)
    assert ready[0].source_run_id == str(run)
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd packages/expert-work-persistence && uv run pytest -k enqueue_persists_source_run_id -q`
Expected: FAIL(`enqueue() got an unexpected keyword argument 'source_run_id'`)。

- [ ] **Step 3: abstract 签名**

`dlq.py` `MemoryWritebackDLQ.enqueue` 抽象方法签名在 `source_thread_id: str | None,` 之后加 `source_run_id: str | None = None,`。

- [ ] **Step 4: in-memory `enqueue`**

`InMemoryMemoryWritebackDLQ.enqueue` 签名同加 `source_run_id: str | None = None,`;构造 `DLQRow(...)` 时加 `source_run_id=source_run_id,`(在 `source_thread_id=` 之后)。

- [ ] **Step 5: sql `enqueue`**

`SqlMemoryWritebackDLQ.enqueue` 签名同加 `source_run_id: str | None = None,`;构造 `MemoryWritebackDLQRow(...)` 时加 `source_run_id=source_run_id,`。

- [ ] **Step 6: 跑测试确认通过**

Run: `cd packages/expert-work-persistence && uv run pytest -k "source_run_id" -q`
Expected: PASS。

- [ ] **Step 7: commit**

```bash
git add packages/expert-work-persistence/src/expert_work/persistence/memory/dlq.py packages/expert-work-persistence/tests/
git commit -m "feat(memory): P5b DLQ enqueue 透传 source_run_id(两 impl)"
```

---

### Task 4: DLQ 溯源接线 —— flush enqueue 传 run_id + worker 重建填

**Files:**
- Modify: `services/orchestrator/src/orchestrator/graph_builder/memory.py`（`flush_messages_to_memory` 的 `dlq.enqueue` 调用)
- Modify: `services/control-plane/src/control_plane/memory/dlq_worker.py`（`_build_memory_items` + `DLQRowLike`)
- Test: `services/orchestrator/tests/test_memory_nodes.py`(flush 失败入队带 run_id)+ `services/control-plane/tests/`(worker 重建带 source_run_id;先探明 dlq_worker 测试文件)

**Interfaces:**
- Consumes: `enqueue(source_run_id=...)`(Task 3)、`DLQRow.source_run_id`(Task 2)、`flush_messages_to_memory` 的 `run_id`(P5b-1)。
- Produces: flush 失败入队时透传 `source_run_id=run_id`;worker `_build_memory_items` 用 `source_run_id=row.source_run_id` 重建 `MemoryItem`;`DLQRowLike` protocol 加 `source_run_id: str | None`。

- [ ] **Step 1: 写失败测试(orchestrator 侧 —— flush 失败入队带 run_id)**

在 `test_memory_nodes.py` 加(用一个会让 write 失败的 store + in-memory DLQ,断言入队行带 source_run_id):

```python
@pytest.mark.asyncio
async def test_flush_failure_enqueues_with_source_run_id() -> None:
    from collections.abc import Sequence
    from uuid import uuid4

    from langchain_core.messages import AIMessage, HumanMessage

    from expert_work.persistence.memory.dlq import InMemoryMemoryWritebackDLQ
    from expert_work.runtime.cancellation import CancellationToken
    from orchestrator.graph_builder.memory import flush_messages_to_memory

    tenant, user, thread, run = uuid4(), uuid4(), uuid4(), uuid4()

    class _BoomStore:
        async def write(self, items: object) -> None:
            raise RuntimeError("db down")

    async def _extract_llm(*, messages: object, tools: object) -> AIMessage:
        del messages, tools
        return AIMessage(content='{"memories": [{"kind": "fact", "content": "x", "importance": 0.9, "confidence": 0.9}]}')

    class _Embedder:
        async def embed(self, texts: Sequence[str], *, tenant_id: object) -> list[tuple[float, ...]]:
            del tenant_id
            return [(1.0, 0.0, 0.0) for _ in texts]

    dlq = InMemoryMemoryWritebackDLQ()
    await flush_messages_to_memory(
        [HumanMessage(content="hi")],
        memory_store=_BoomStore(),  # type: ignore[arg-type]
        embedder=_Embedder(),  # type: ignore[arg-type]
        llm_caller=_extract_llm,  # type: ignore[arg-type]
        tenant_id=tenant, user_id=user, thread_id=thread, run_id=str(run),
        token=CancellationToken(), dlq=dlq,
    )
    assert await dlq.count() == 1
    enqueued = next(iter(dlq._rows.values()))  # type: ignore[attr-defined]
    assert enqueued.source_run_id == str(run)
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd services/orchestrator && uv run pytest tests/test_memory_nodes.py -k flush_failure_enqueues -q`
Expected: FAIL(`enqueued.source_run_id` 为 None —— flush 的 enqueue 调用未传 run_id)。

- [ ] **Step 3: flush 的 enqueue 调用透传 run_id**

`memory.py` `flush_messages_to_memory` 里的 `dlq.enqueue(...)` 调用(现传 `tenant_id/user_id/source_thread_id/extracted/error`)加 `source_run_id=run_id,`:

```python
                await dlq.enqueue(
                    tenant_id=tenant_id,
                    user_id=user_id,
                    source_thread_id=str(thread_id) if thread_id is not None else None,
                    source_run_id=run_id,
                    extracted=[(str(m.kind), m.content) for m in extracted],
                    error=f"{type(exc).__name__}: {exc}",
                )
```

并把 `flush_messages_to_memory` docstring 里 P5b-1 那句"DLQ-retry path does not carry run_id yet"更新为"DLQ-retry path now carries run_id (P5b-2a)"。

- [ ] **Step 4: worker 重建填 source_run_id**

`dlq_worker.py`:
1. `_build_memory_items` 的 `MemoryItem(...)` 构造加 `source_run_id=row.source_run_id,`(在 `source_thread_id=row.source_thread_id,` 之后)。
2. `DLQRowLike` protocol(structural duck-type)加 `source_run_id: str | None`(在 `source_thread_id: str | None` 之后)。

- [ ] **Step 5: 写 worker 侧测试(重建带 source_run_id)**

先探明 dlq_worker 测试文件:`grep -rln "_build_memory_items\|dlq_worker\|MemoryWritebackWorker" services/control-plane/tests/`。在该文件加一个断言 `_build_memory_items` 用一个带 `source_run_id` 的 `DLQRow`(或 DLQRowLike stub)产出的 `MemoryItem.source_run_id` 正确的测试。若测试已有构造 DLQRow-like 的 helper,复用它加 `source_run_id`。

```python
def test_build_memory_items_carries_source_run_id() -> None:
    # 用文件既有的 DLQRow/stub 构造方式,带 source_run_id="run-123",
    # 断言产出的每个 MemoryItem.source_run_id == "run-123"。
    ...  # 按既有测试范式填充(extracted 两条 → 两个 item 均带 source_run_id)
```

> 实现者:读该测试文件的既有 `_build_memory_items` 测试,照其 DLQRow 构造 + vectors 构造范式补 `source_run_id`;不要新造 stub 若已有。

- [ ] **Step 6: 跑测试确认通过(两侧)**

Run: `cd services/orchestrator && uv run pytest tests/test_memory_nodes.py -k flush_failure -q`
Run: `cd services/control-plane && uv run pytest tests/ -k "build_memory_items or source_run_id" -q`
Expected: 均 PASS。

- [ ] **Step 7: commit**

```bash
git add services/orchestrator/src/orchestrator/graph_builder/memory.py services/control-plane/src/control_plane/memory/dlq_worker.py services/orchestrator/tests/test_memory_nodes.py services/control-plane/tests/
git commit -m "feat(memory): P5b DLQ 溯源接线 —— flush 入队传 run_id + worker 重建填"
```

---

## 终检(全部 task 后)

- [ ] 两 store parity:as_of 分支语义 sql 与 in-memory 一致(current + as_of 两路)。
- [ ] `uv run ruff check packages services` + `uv run ruff format --check packages services`。
- [ ] CI-scope mypy:`uv run mypy packages services/audit-backup-worker/src services/billing-rollup-job/src services/event-log-archive-job/src services/orchestrator/src services/retention-cleanup-job/src`。
- [ ] control-plane 回归(改了 dlq_worker + flush enqueue):`cd services/control-plane && uv run pytest -q`。
- [ ] 持久层全量(含集成,DOCKER_HOST):`cd packages/expert-work-persistence && uv run pytest -q` —— migration 链 0125→…→0128 单 head;既有 flake(pgbouncer/RLS 隔离)非本改动。
- [ ] orchestrator 全量:`cd services/orchestrator && uv run pytest -q`(1 pg-checkpoint docker env 错既有)。

## 风险 / 注意

- as_of 目前无生产调用者(recall/reconcile 传 None)——本 PR 落 store 能力 + 测试,API/前端暴露在 P5b-2c。像 P5a rewrite/abstain 先落 node 层再接。
- as_of 仍排除 `deleted_at`(隐私:遗忘=彻底不返,即使时间旅行)——刻意,非缺陷。
- DLQ 溯源:历史 dlq 行 `source_run_id` NULL(migration 前入队的),worker 重建它们时 `source_run_id=None` —— 可接受(旧行没这信息)。
- P5b-2b(单独 plan):⑦ `expected_valid_days` 主动预测复核 + ⑧ 抽取 prompt correction 标记。P5b-2c:前端记忆 tab 跳 run + as_of 查历史(+ 把 as_of/source_run_id 暴露给 memory API)。
